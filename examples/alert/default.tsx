import { Alert, Button } from "@ship-it-ui/ui";

export default function Example() {
    return <Alert tone='accent' title='Schema preview ready' description='Review proposed entity types before we commit them to your graph.' />;
}

