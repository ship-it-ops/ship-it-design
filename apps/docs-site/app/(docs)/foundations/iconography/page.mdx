import { IconGlyph } from '@ship-it-ui/icons';

# Iconography

The icon vocabulary is **inline SVG via `<IconGlyph>`** — every name resolves
to an Iconify-sourced SVG (Lucide for UI primitives, Phosphor for soft
variants, simple-icons for brand logos). Rendered at text weight,
monochromatic, in `currentColor`, always centered in a generous square.

The names live in `packages/icons/src/icon-manifest.ts`; swap a single tuple
there to change the icon everywhere it's used. Generated SVG bodies are
materialised into `src/icon-data.ts` at build time so the runtime has zero
network dependency.

## Full inventory

export const inventory = [
  { name: 'brand', use: 'brand mark' },
  { name: 'service', use: 'service' },
  { name: 'person', use: 'person' },
  { name: 'document', use: 'document' },
  { name: 'file', use: 'file' },
  { name: 'ticket', use: 'ticket' },
  { name: 'deployment', use: 'deployment' },
  { name: 'ask', use: 'ask / ai' },
  { name: 'incident', use: 'incident' },
  { name: 'target', use: 'target' },
  { name: 'live', use: 'live / record' },
  { name: 'half', use: 'half' },
  { name: 'dot', use: 'dot' },
  { name: 'bolt', use: 'bolt' },
  { name: 'graph', use: 'graph' },
  { name: 'schema', use: 'schema' },
  { name: 'home', use: 'home' },
  { name: 'fitView', use: 'fit-view' },
  { name: 'expand', use: 'expand' },
  { name: 'collapse', use: 'collapse' },
  { name: 'next', use: 'next' },
  { name: 'prev', use: 'prev' },
  { name: 'up', use: 'up' },
  { name: 'down', use: 'down' },
  { name: 'upRight', use: 'up-right' },
  { name: 'downRight', use: 'down-right' },
  { name: 'external', use: 'external' },
  { name: 'enter', use: 'enter' },
  { name: 'sort', use: 'sort' },
  { name: 'caretLeft', use: 'left' },
  { name: 'caretRight', use: 'right' },
  { name: 'caretUp', use: 'caret-up' },
  { name: 'caretDown', use: 'caret-down' },
  { name: 'confirm', use: 'confirm' },
  { name: 'close', use: 'close' },
  { name: 'add', use: 'add' },
  { name: 'remove', use: 'remove' },
  { name: 'warn', use: 'warn' },
  { name: 'help', use: 'help' },
  { name: 'info', use: 'info' },
  { name: 'more', use: 'more' },
  { name: 'moreVertical', use: 'more-vertical' },
  { name: 'edit', use: 'edit' },
  { name: 'copy', use: 'copy' },
  { name: 'refresh', use: 'refresh' },
  { name: 'search', use: 'search' },
  { name: 'cmd', use: 'cmd' },
  { name: 'shift', use: 'shift' },
  { name: 'option', use: 'option' },
  { name: 'escape', use: 'escape' },
  { name: 'settings', use: 'settings' },
  { name: 'power', use: 'power' },
  { name: 'mention', use: 'mention' },
  { name: 'tag', use: 'tag' },
  { name: 'approx', use: 'approx' },
  { name: 'infinite', use: 'infinite' },
  { name: 'section', use: 'section' },
  { name: 'kubernetes', use: 'kubernetes' },
  { name: 'datadog', use: 'datadog' },
  { name: 'backstage', use: 'backstage' },
  { name: 'pagerduty', use: 'pagerduty' },
  { name: 'github', use: 'github' },
];

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(8, 1fr)', gap: 8 }}>
  {inventory.map((x) => (
    <div
      key={x.name}
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 4,
        padding: '10px 6px',
        background: 'var(--color-panel-2)',
        border: '1px solid var(--color-border)',
        borderRadius: 8,
      }}
    >
      <IconGlyph name={x.name} size={22} style={{ color: 'var(--color-text)' }} />
      <div
        style={{
          fontSize: 9,
          color: 'var(--color-text-dim)',
          fontFamily: 'var(--font-family-mono)',
          textAlign: 'center',
        }}
      >
        {x.use}
      </div>
    </div>
  ))}
</div>

## Connector logos

Brand marks for first-class data sources. Use `<IconGlyph kind="connector" />`.

export const connectors = [
  'github',
  'notion',
  'slack',
  'linear',
  'jira',
  'pagerduty',
  'confluence',
  'gdrive',
  's3',
  'postgres',
];

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(5, 1fr)', gap: 8 }}>
  {connectors.map((name) => (
    <div
      key={name}
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 4,
        padding: '10px 6px',
        background: 'var(--color-panel-2)',
        border: '1px solid var(--color-border)',
        borderRadius: 8,
      }}
    >
      <IconGlyph kind="connector" name={name} size={22} style={{ color: 'var(--color-text)' }} />
      <div
        style={{
          fontSize: 9,
          color: 'var(--color-text-dim)',
          fontFamily: 'var(--font-family-mono)',
          textAlign: 'center',
        }}
      >
        {name}
      </div>
    </div>
  ))}
</div>

## Sizes

<div style={{ display: 'flex', gap: 18, alignItems: 'center', padding: '12px 0' }}>
  {[12, 14, 16, 20, 24, 32, 48].map((s) => (
    <div key={s} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 4 }}>
      <IconGlyph name="ask" size={s} style={{ color: 'var(--color-text)' }} />
      <span
        style={{
          fontSize: 9,
          color: 'var(--color-text-dim)',
          fontFamily: 'var(--font-family-mono)',
        }}
      >
        {s}px
      </span>
    </div>
  ))}
</div>

## Treatments

<div style={{ display: 'flex', gap: 14, alignItems: 'flex-end' }}>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 4 }}>
    <IconGlyph name="ask" size={20} />
    <span
      style={{
        fontSize: 10,
        color: 'var(--color-text-dim)',
        fontFamily: 'var(--font-family-mono)',
      }}
    >
      plain
    </span>
  </div>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 4 }}>
    <IconGlyph name="ask" size={20} style={{ color: 'var(--color-text-dim)' }} />
    <span
      style={{
        fontSize: 10,
        color: 'var(--color-text-dim)',
        fontFamily: 'var(--font-family-mono)',
      }}
    >
      muted
    </span>
  </div>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 4 }}>
    <IconGlyph name="ask" size={20} style={{ color: 'var(--color-accent)' }} />
    <span
      style={{
        fontSize: 10,
        color: 'var(--color-text-dim)',
        fontFamily: 'var(--font-family-mono)',
      }}
    >
      accent
    </span>
  </div>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 4 }}>
    <div
      style={{
        width: 28,
        height: 28,
        borderRadius: 6,
        background: 'var(--color-panel-2)',
        border: '1px solid var(--color-border)',
        display: 'grid',
        placeItems: 'center',
        color: 'var(--color-accent)',
      }}
    >
      <IconGlyph name="ask" size={14} />
    </div>
    <span
      style={{
        fontSize: 10,
        color: 'var(--color-text-dim)',
        fontFamily: 'var(--font-family-mono)',
      }}
    >
      in chip
    </span>
  </div>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 4 }}>
    <div
      style={{
        width: 28,
        height: 28,
        borderRadius: 999,
        background: 'var(--color-accent-dim)',
        color: 'var(--color-accent)',
        display: 'grid',
        placeItems: 'center',
      }}
    >
      <IconGlyph name="ask" size={14} />
    </div>
    <span
      style={{
        fontSize: 10,
        color: 'var(--color-text-dim)',
        fontFamily: 'var(--font-family-mono)',
      }}
    >
      in circle
    </span>
  </div>
  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 4 }}>
    <div
      style={{
        width: 32,
        height: 32,
        borderRadius: 10,
        background: 'var(--color-panel)',
        border: '1.5px solid var(--color-accent)',
        boxShadow: '0 0 20px var(--color-accent-glow)',
        color: 'var(--color-accent)',
        display: 'grid',
        placeItems: 'center',
      }}
    >
      <IconGlyph name="ask" size={16} />
    </div>
    <span
      style={{
        fontSize: 10,
        color: 'var(--color-text-dim)',
        fontFamily: 'var(--font-family-mono)',
      }}
    >
      glowing
    </span>
  </div>
</div>

## Usage

```tsx
import { IconGlyph } from '@ship-it-ui/icons';

<IconGlyph name="ask" size={14} />
<IconGlyph name="service" size={22} />
<IconGlyph kind="connector" name="github" size={20} />
```

`<IconGlyph>` is the canonical surface — it handles sizing, ARIA, and
`currentColor` propagation. For runtime-computed names use
`<DynamicIconGlyph>`, which accepts any string and falls back to centered
SVG text when no icon is registered.

For non-React surfaces (cytoscape `background-image`, canvas, Mermaid), call
`iconToSvgDataUrl(name, { color, size })` for a ready
`data:image/svg+xml;…` URL — used internally by the cytoscape adapter to
paint entity glyphs on graph nodes.
