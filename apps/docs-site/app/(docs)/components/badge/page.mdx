# Badge

Each example below is a real component instance — flip the theme toggle to see how it adapts.

## Default

<LivePreview example="badge/default" />

## Variants

<LivePreview example="badge/variants" />

## With Dots

<LivePreview example="badge/with-dots" />

## Sizes

<LivePreview example="badge/sizes" />

## Custom color

Pass any CSS color via the `color` prop for one-off overrides outside the semantic variants. When set, `color` takes precedence over `variant` at runtime. Invalid colors fall back to the default variant; in dev, a `console.warn` names the offending value.

<LivePreview example="badge/custom-color" />

## Props

<PropsTable component="Badge" />

<EditOnGithub source="apps/docs-site/app/(docs)/components/badge/page.mdx" />
