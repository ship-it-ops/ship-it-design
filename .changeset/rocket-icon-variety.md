---
'@ship-it-ui/icons': patch
---

Add a rocket glyph variety pack: canonical `rocket` (Lucide) plus 24 variants
across Phosphor (`rocketAlt`/`rocketThin`/`rocketLight`/`rocketBold`/`rocketFill`/
`rocketDuotone` and the diagonal `rocketLaunch` family in the same six weights)
and Solar (`rocket*` and tilted `rocket2*` families in Linear, Outline, Broken,
Solid, SolidDuotone, and LineDuotone styles). Also add the Rocket.rs framework
logo as `rocket` in the logo set.
