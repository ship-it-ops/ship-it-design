---
'@ship-it-ui/map': patch
---

Bump devDependency `maplibre-gl` 4.7.1 → 5.24.0. The package's
peerDependency range already declared `^4.0.0 || ^5.0.0` — consumers
on v5 were already supported. No source changes; build output
unchanged.
